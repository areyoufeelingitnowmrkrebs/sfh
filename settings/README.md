/settings

Everything is configured in here. No bloat or hidden boilerplate—just a clean collection of directories named directly after the programs they configure, including the kernel itself. 

As a design rule, `/settings` defines system-wide defaults for the root account and new user baselines whereas individual user divergences are handled via `~/preferences`.
