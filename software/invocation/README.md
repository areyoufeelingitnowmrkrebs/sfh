/software/invocation

Primary entry point for execution. Contains no raw binaries, only symbolic links to executables housed within `/software/programs` or `/software/applications`, keeping the system `$PATH` singular and pristine.
